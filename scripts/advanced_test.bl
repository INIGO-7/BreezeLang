d[ square_or_sum(a, is_squared) ->
  i[ is_squared ->
    a = a ** 2;
  e[ ->
    a = a + 1;
  ];
  print "if is_squared is true, a = 9. If not, a = 4. is_squared:";
  print is_squared;
  print "a's value:";
  print a;
];

square_or_sum(3, false);
